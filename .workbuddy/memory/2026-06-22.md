# 2026-06-22 工作日志

## 墨间 AI 剧本工具 — 落地页接入与全局配色改造

### 任务1：路由整合（落地页作为入口）
- `app/page.tsx` → 改为墨间落地页（原 `page1.tsx` 内容），CTA（"开始创作"/"开始你的第一段故事"）跳转 `/home`；导航为锚点 (#flow/#features/#footer)。
- `app/home/page.tsx` → 新建，承载原剧集列表首页，头部加「墨间」logo 链回 `/`。
- `app/episode/[id]/page.tsx` → 返回按钮与未找到页"返回首页"均改为 `/home`。
- 删除 `app/page1.tsx`（已并入 page.tsx）。
- 路由结构：`/`(落地页) → `/home`(剧集列表) → `/episode/[id]`(编辑器)。

### 任务2：全局配色/样式改为墨间暖色系
- `tailwind.config.ts`：把 `brand` 与 `slate` 两个色阶整体重映射为暖色（brand 400/500=琥珀 #D97706/#B45309，600/700=深墨 #44403C/#3D3935；slate 50-900 映射为暖灰 warm 系）。一处改配置即可让所有 `brand-*`/`slate-*` 类自动换色。
- 修复既有 type 错误：`boxShadow` 数组值改为逗号拼接字符串（原配置在 dev 下不报错，build 才暴露）。
- `app/globals.css`：滚动条改暖色；新增全局 `h1,h2,h3 { font-family: var(--font-serif) }`。
- `app/layout.tsx`：加载 Noto Serif SC / Noto Sans SC（Google Fonts link），更新 title 为"墨间 · AI 剧本开发工具"。
- 业务组件语义色外科手术式替换：
  - `TaggedText` @标签 蓝→琥珀 `text-amber-700`。
  - `AssetPreparation`/`VideoGeneration` TYPE_BADGE_CLASS 改用设计稿 tag 色（amber/sage/gold）；STATUS_BADGE_CLASS running→琥珀、succeeded→emerald；蓝/紫信息框→琥珀；green-* → emerald-*。
  - `StoryboardTable` "已标注"badge 蓝→琥珀。
  - `SettingsModal` 内联 hex（#4338ca/#334155/#6366f1/#1e293b/#cbd5e1）→暖色/琥珀；紫信息框→琥珀；绿状态→emerald。
  - `Button` 主按钮文字 `text-warm-50`、圆角 `rounded-lg`。
- `next build` 通过；dev 服务跑在 3300 端口（3000 被占用），`/` 与 `/home` 均 200。

### 关键决策
- 用「重定义 brand+slate 色阶」而非逐文件改类名，最小改动覆盖 ~80% 配色，剩余语义色单独改。
- 登录功能按用户要求暂不处理，"登录"保留为 `#` 占位。

### 任务3：模型列表手动更新功能
- 新增 `lib/model-presets.ts`：统一管理 LLM / 图片 / 视频三类模型的列表（localStorage 持久化），优先读用户自定义、fallback 硬编码默认值，支持 get/save/reset。
- 更新 `components/SettingsModal.tsx`：为 LLM / 图片 / 视频的模型选择器各加「管理模型」按钮，点击展开内联编辑面板，支持添加新模型（ID + 可选显示名）、删除已有模型、恢复默认列表。
- `next build` 通过。

### 热更新修复（晚间）
- 问题：dev server 未运行，`.next` 残留 build 产物干扰 HMR。
- 措施：清理 `.next` → 优化 `next.config.mjs` webpack watchOptions（`poll: 800` + `aggregateTimeout: 300` + 扩展 ignored 列表含 `.workbuddy`）→ 添加 `dev:turbo` 脚本 → 以 Turbopack 模式在 3300 端口启动，200 正常。

### 任务4：接入腾讯云 COS 存储
- 背景：Step4 视频生成时，关联资产图片需作为参考帧传给 Seedance API，但之前图片存为 base64/blob URL，API 无法访问。
- 安装 `cos-nodejs-sdk-v5`。
- 新增 `lib/cos-client.ts`：COS 配置管理（getCosSettings/saveCosSettings/isCosConfigured/buildCosPublicUrl），存 localStorage key `mojian_cos_settings`。
- `lib/types.ts`：新增 `CosSettings` 接口（secretId/secretKey/bucket/region/customDomain?）。
- 新建 `app/api/cos/upload/route.ts`：接收 base64 + CosSettings，动态 import COS SDK，putObject 到 `ai-script/assets/{ts}-{rand}.{ext}`，返回公网 URL（支持自定义域名）。
- 更新 `SettingsModal.tsx`：新增「腾讯云 COS 存储」折叠区域（SecretId/SecretKey/Bucket/Region/自定义域名），含测试上传（1px PNG）功能。
- 更新 `AssetPreparation.tsx`：资产卡片新增「上传」按钮（仅 COS 已配置时显示），FileReader 读 base64 → 调 /api/cos/upload → 写回 imageUrl，上传中独立 spinner 状态。
- 更新 `VideoGeneration.tsx`：移除 base64 过滤逻辑 `!u.startsWith("data:")`（COS URL 都是标准公网 URL，无需过滤）。
- `next build` 通过。
