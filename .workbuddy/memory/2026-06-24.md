# 2026-06-24 工作日志

## Step4 视频生成页功能增强

完成以下三项需求（来自截图红字标注）：

1. **关联资产可删除** — `VideoGeneration.tsx` 中每个关联资产卡片右上角增加 `×` 删除按钮，点击调用 `onDeleteAsset`，资产从 `episode.assets` 中移除。

2. **Step4 页面内添加资产** — 顶栏增加「+ 添加资产」按钮，内联展开表单（名称输入 + 类型下拉），逻辑与 Step3 一致，调用新 `onAddAsset` 回调。

3. **视频提示词 @ 下拉补全** — `EditableCell.tsx` 新增 `atMentionOptions` prop；multiline 模式下检测光标前 `@` 触发，弹浮动下拉框；支持 ↑↓/Enter/Tab/Escape 键盘导航；选中后替换文本中的 `@xxx`。

改动文件：
- `components/EditableCell.tsx` — 新增 @ 下拉逻辑
- `components/VideoGeneration.tsx` — 新增 props + 添加/删除资产 UI
- `app/episode/[id]/page.tsx` — 新增 `handleDeleteAsset`/`handleAddAsset`，传给 VideoGeneration

TypeScript `tsc --noEmit` 检查通过，无类型错误。

4. **关联资产与 @ 同步** — `getRelatedAssets()` 改为同时扫描 `visualDescription` + `finalPrompt`（视频提示词）。用户在提示词中通过 @ 补全引用资产后，该资产自动出现在关联资产列表中，不再仅依赖画面描述的 @标签。修改处：`VideoGeneration.tsx` 第 99-125 行。

5. **关联资产 × 按钮改为「解除关联」而非「删除资产」** — 核心架构调整：
   - `Shot` 类型新增 `relatedAssetIds: string[]` 字段，持久化记录每个镜头关联的资产
   - `getRelatedAssets()` 改为基于 `relatedAssetIds` 查找资产，不再动态扫描文本
   - `×` 按钮调用 `onUnlinkAsset(shotId, assetId)`，从该镜头关联列表中移除，**不删除资产本身**
   - `@` 补全选中资产时，通过 `onAtMentionSelect` → `onLinkAsset(shotId, assetId)` 自动将资产 ID 加入该镜头关联列表
   - `normalizeEpisode` / `emptyShot` / `toShot` 补全 `relatedAssetIds` 默认值兼容旧数据
   改动文件：
   - `lib/types.ts` — Shot 增加 `relatedAssetIds`
   - `lib/utils.ts` — `emptyShot`/`toShot`/`normalizeEpisode` 补全字段
   - `components/EditableCell.tsx` — 新增 `onAtMentionSelect` prop
   - `components/VideoGeneration.tsx` — 重构关联逻辑，新增 `onLinkAsset`/`onUnlinkAsset` props
   - `app/episode/[id]/page.tsx` — 新增 `handleLinkAsset`/`handleUnlinkAsset`

6. **移除 Step3 和 Step4 的「+ 添加资产」功能**：
   - Step3 (`AssetPreparation.tsx`)：移除 `showAddForm/newAssetName/newAssetType` 状态、「+ 添加资产」按钮、内联表单、`handleAddAsset()` 函数。资产只能通过 Step2 的 @标签 → 一键生成来创建。
   - Step4 (`VideoGeneration.tsx`)：同上，移除所有添加资产相关代码及 `onDeleteAsset`/`onAddAsset` prop。用户通过视频提示词的 @ 补全自动关联已有资产。
   - `page.tsx`：移除 `handleDeleteAsset`/`handleAddAsset` 及传给 VideoGeneration 的对应 prop。

7. **Step3 资产网格末尾增加「添加资产」占位卡片** — 按照截图红框标注，在资产卡片的 grid 网格末尾放置一个虚线边框的空占位卡片（`+ 添加资产`），点击后展开为内联表单（名称输入 + 类型下拉 + 确认/取消按钮），确认后将新资产加入 `episode.assets`。样式与现有 AssetCard 保持一致的 4:3 宽高比和圆角。
   改动文件：`components/AssetPreparation.tsx`

8. **「世界设定」功能** — 剧集列表页右上角新增「世界设定」按钮，点击弹出模态框，可配置 ①故事背景 ②核心主题 ③写作风格。设定自动注入到 Step1 故事扩写和 Step2 分镜生成的 LLM 上下文。
   新增文件：
   - `lib/world-settings.ts` — get/save/worldSettingsToText/hasWorldSettings
   - `components/WorldSettingsModal.tsx` — 弹窗组件
   修改文件：
   - `lib/types.ts` — 新增 WorldSettings 接口
   - `lib/prompts.ts` — expansionMessages/storyboardMessages 新增 worldText 参数
   - `components/ContentExpansion.tsx` — 调用时传入世界设定文本
   - `app/home/page.tsx` — header 新增「世界设定」按钮 + 模态框
   `next build` 通过。

9. **首页「新建剧集」按钮改为网格内占位卡片** — 按截图红字标注：
   - `app/home/page.tsx`：移除顶栏右侧 `<Button>+ 新建剧集</Button>`；将 `handleNew` 作为 `onCreate` prop 传给 EpisodeList
   - `components/EpisodeList.tsx`：新增 `onCreate?` prop；空状态改为显示一个虚线边框的「+ 新建剧集」卡片；有剧集时在网格末尾追加同样的占位卡片，hover 时边框/文字变品牌色

10. **剧集顺序 + 扩写带入前集内容**：
   - `Episode` 类型新增 `order: number` 字段（从 1 开始）
   - `emptyEpisode(order)` 接受序号参数；`listEpisodes()` 按 `order` 正序排列；旧数据首次加载时自动补全序号
   - 首页卡片和编辑页标题旁显示「第 X 集」标签
   - 扩写时自动拼接前几集扩写内容（总计 ≤3000 字，超出优先保留最近几集），通过 `expansionMessages` 的 `previousEpisodesContext` 参数传入
   - 分镜生成不带前集内容（与用户确认，无需带入）

11. **漫剧风格预设功能** — 全局风格选择，贯穿资产/图片/视频生成全链路：
   - `lib/types.ts`：新增 `StylePreset`（id/name/description/characterTemplate/sceneTemplate/objectTemplate/videoStyleSuffix）和 `StyleSettings`（selectedStyleId/overrides）
   - `lib/style-settings.ts`（新建）：4 个预设风格（真人/2D动漫/国产剧/水墨），存储层 get/save/getActive/getAssetTemplate/getVideoStyleSuffix/styleToText/resetOverride
   - `components/StyleSettingsModal.tsx`（新建）：风格卡片选择（2×2 网格）+ 4 个模板编辑 textarea + 恢复默认/保存
   - `app/home/page.tsx`：header 新增「漫剧风格」按钮 + StyleSettingsModal
   - `lib/prompts.ts`：`assetMessages` 增加 `styleText` 参数注入风格到 LLM system prompt；`videoPromptMessages` 增加 `videoStyleSuffix` 参数
   - `components/AssetPreparation.tsx`：`handleGenerateAll` 传入风格文本；`generateImageForAsset` 拼接 `getAssetTemplate(asset.type)` 到图片提示词末尾
   - `components/VideoGeneration.tsx`：`generateOne`/`generateAll` 传入视频风格后缀；`generateVideo` 拼接后缀到 finalPrompt
   - 人物模板含三视图要求（正面/侧面/背面），拼接到 LLM 生成的 imagePrompt 末尾
   - localStorage key: `mojian_style_settings`

12. **风格优化迭代**：
   - 国产真人剧 → 国产漫剧风格 → 国漫修仙风格（参考《凡人修仙传》），模板全面重写为3D修仙质感
   - 移除所有模板中的「暗色调配金银点缀」
   - 视频后缀精简为核心风格方向，不再强制限定色调

13. **SettingsModal Bug 修复 — 统一保存按钮**：
   - 问题：4 个面板各有独立保存按钮，LLM 保存后 `setTimeout(() => onClose(), 600)` 自动关闭弹窗，导致其他面板未保存的配置丢失
   - 修复：移除所有独立保存按钮，底部改为统一的「保存全部设置」按钮，一次性保存 LLM+图片+视频+COS+所有模型列表，保存后不自动关闭
