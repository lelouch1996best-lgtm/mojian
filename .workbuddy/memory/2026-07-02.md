# 2026-07-02 工作日志

## SQLite 服务端存储迁移（完成）

将墨间从纯 localStorage 迁移到 SQLite 服务端存储，通过环境变量切换模式。

### 新文件（9 个）
- `lib/db.ts` — SQLite 单例连接（better-sqlite3）+ 建表 DDL
- `lib/auth.ts` — Bearer Token 鉴权（开发模式跳过）
- `lib/api-client.ts` — 前端 fetch 封装（统一 JSON + Auth 头）
- `app/api/data/series/route.ts` — 系列 GET 列表 / POST upsert
- `app/api/data/series/[id]/route.ts` — 系列 GET / DELETE
- `app/api/data/episodes/route.ts` — 剧集 GET?seriesId / POST upsert
- `app/api/data/episodes/[id]/route.ts` — 剧集 GET / DELETE
- `app/api/settings/[key]/route.ts` — 设置 GET / PUT
- `app/api/migrate/route.ts` — 批量导入迁移

### 数据库表
- `series` — id/title/description/order_num/world_settings(JSON)/style_settings(JSON)/episode_order(JSON)
- `episodes` — id/title/series_id/step/original_content/expanded_content/shots(JSON)/assets(JSON)
- `settings` — key-value 模式（llm/image/video/cos/world/style/models_llm/models_image/models_video）

### lib 文件全量异步化（8 个）
所有 `lib/` 存储函数改为 `async`，通过 `NEXT_PUBLIC_STORAGE_MODE` 环境变量切换：
- `lib/storage.ts` — Series/Episode CRUD → async + server 分支
- `lib/llm-client.ts` — getSettings/saveSettings → async + server 分支；buildBody → async
- `lib/image-client.ts` — getImageSettings/saveImageSettings → async + server 分支
- `lib/video-client.ts` — getVideoSettings/saveVideoSettings/isVideoConfigured → async + server 分支
- `lib/cos-client.ts` — getCosSettings/saveCosSettings/isCosConfigured → async + server 分支
- `lib/world-settings.ts` — getWorldSettings/saveWorldSettings/hasWorldSettings → async + server 分支
- `lib/style-settings.ts` — getStyleSettings/saveStyleSettings/getActiveStyle/getAssetTemplate/getVideoStyleSuffix/getAllStyles/resetStyleOverride/hasStyleOverride → async + server 分支
- `lib/model-presets.ts` — getLLMModels/saveLLMModels/resetLLMModels + image/video 版本 → async + server 分支

### UI 组件适配（~10 个）
- `app/home/page.tsx` — refresh/handleNew 加 await
- `app/series/[id]/page.tsx` — refresh/handleNewEpisode/handleDeleteEpisode 加 await
- `app/episode/[id]/page.tsx` — useEffect 改为 async IIFE；persist 改为 async debounce
- `components/SettingsModal.tsx` — useEffect 内全量 await；handleProviderChange/handleAddLLMModel/handleDeleteLLMModel/handleResetLLMModels/handleAddImageModel/handleDeleteImageModel/handleResetImageModels/handleAddVideoModel/handleDeleteVideoModel/handleResetVideoModels/handleSaveAll 改为 async
- `components/WorldSettingsModal.tsx` — useEffect 加 async IIFE + await
- `components/StyleSettingsModal.tsx` — useEffect 加 async IIFE + await
- `components/ContentExpansion.tsx` — worldText 改为 useEffect + useState
- `components/AssetPreparation.tsx` — cosConfigured/imageConfigured 改为 useState + useEffect
- `components/VideoGeneration.tsx` — videoConfigured 改为 useState + useEffect

### 配置
- `.env.local` — `NEXT_PUBLIC_STORAGE_MODE=local`
- `.gitignore` — 添加 `data/` 和 `.env.production`
- `next.config.mjs` — 移除 `serverExternalPackages`（Next 14 不支持）

### 测试
- `tsc --noEmit` 全部通过
- dev server `next dev -p 3300` 启动正常，落地页和企划列表页均返回 200
