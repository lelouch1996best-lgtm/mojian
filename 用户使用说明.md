# 墨间用户使用说明

## 产品简介

墨间是一款面向短剧、漫剧和视频创作者的 AI 剧本开发工具。它把故事扩写、分镜拆解、角色/场景/物品资产准备、图片生成、视频生成、音乐生成和素材管理串成一条完整创作流程，适合从一个剧情灵感逐步推进到可用于成片制作的分镜和素材。

用户在墨间中以“企划”为单位组织作品。每个企划可以包含多集剧集、独立的世界观、人物设定、物品设定、场景设定、风格设定和音乐内容。

## 使用前准备

### 启动应用

如果以网页开发模式运行：

```bash
npm install
npm run dev
```

启动后在浏览器访问：

```text
http://localhost:3000
```

如果使用桌面客户端，请直接打开已构建好的墨间客户端。

### 首次配置

首次使用前建议先进入首页右上角的“设置”页面，完成必要的 API 与存储配置。

| 配置项 | 用途 | 是否必需 |
| --- | --- | --- |
| LLM 设置 | 用于故事扩写、分镜生成、智能标注、提示词优化、人物提取等文本能力 | 必需 |
| 图片 API | 用于生成角色图、场景图、物品图、故事板图和风格参考图 | 使用图片生成时必需 |
| 视频 API | 用于按镜头生成视频 | 使用视频生成时必需 |
| 音频 API | 用于生成人物音色 | 使用人物音色时必需 |
| 音乐 API | 用于生成歌曲、纯音乐和二次创作音乐 | 使用音乐功能时必需 |
| 腾讯云 COS | 用于上传本地素材、保存生成图片、保存视频和音频的公网地址 | 建议配置 |

在“设置”页面中，填写对应供应商的 `baseURL`、`apiKey`、模型名称和相关参数。部分模型列表支持自定义增删，也可以恢复内置默认模型。

### API Key 批量管理

设置页支持按品牌批量填写 API Key。适合同一个供应商同时用于 LLM、图片、视频、音频或音乐能力的情况。填写后可以选择是否把该品牌设置为各分类默认供应商。

## 首页与企划管理

打开墨间后，首页显示“我的企划”列表。

### 新建企划

1. 点击“新建企划”卡片。
2. 如果尚未配置 LLM，系统会引导进入“设置”页。
3. 创建成功后进入企划详情页。
4. 点击企划标题可以直接修改名称。

### 管理企划

首页中的每张企划卡片展示企划名称、描述、剧集数量和更新时间。点击卡片进入企划，悬停后可使用删除按钮删除企划及其全部剧集。

### 首页快捷入口

首页右上角提供以下入口：

| 入口 | 功能 |
| --- | --- |
| 资产库 | 查看和管理生成或手动添加的图片、视频、音频、音乐、音色等素材 |
| 预设库 | 管理可复用的图片、视频、音频、文本预设素材 |
| 风格模板 | 新建、编辑和管理全局风格模板 |
| 设置 | 配置 LLM、图片、视频、音频、音乐、COS 和默认生成参数 |

## 企划详情

进入某个企划后，可以在“剧集”和“音乐”两个标签之间切换。

### 剧集标签

剧集标签用于管理该企划下的多集内容。

可执行操作：

- 新建剧集。
- 进入剧集编辑器。
- 删除剧集。
- 查看每集更新时间和镜头数量。

### 音乐标签

音乐标签用于管理该企划下的配乐、歌曲或二创音乐。

可执行操作：

- 新建音乐。
- 进入音乐编辑页。
- 删除音乐。
- 查看音乐模式、生成状态、封面、时长和播放器。

### 企划设定入口

企划详情页顶部提供以下设定入口：

| 入口 | 说明 |
| --- | --- |
| 世界设定 | 配置故事背景、核心主题、写作风格 |
| 人物设定 | 管理系列级人物档案和多版本形象 |
| 物品设定 | 管理系列级道具、武器、载具等物品档案 |
| 场景设定 | 管理系列级地点、空间、环境档案 |
| 风格设定 | 为当前企划选择使用的创作风格模板 |

这些设定会在故事扩写、资产生成、图片生成和视频提示词生成中被引用，用于保持系列的一致性。

## 世界设定

世界设定是整个企划的故事基础，包含三个字段：

| 字段 | 用途 |
| --- | --- |
| 故事背景 | 描述时代、地点、世界观、社会环境等 |
| 核心主题 | 描述作品想表达的思想或情绪 |
| 写作风格 | 描述叙事语气、节奏、修辞偏好和画面表达方式 |

填写完成后点击“保存”。后续进行故事扩写时，可以在上下文弹窗中选择引用世界设定。

## 人物设定

人物设定用于维护跨剧集复用的角色档案。

### 人物档案字段

人物档案通常包含：

- 姓名。
- 角色定位。
- 性别年龄。
- 外貌描述。
- 性格特点。
- 背景故事。
- 人物关系。
- 版本标签。
- 人物形象图。
- 参考图。
- 音色音频和音色描述。

### 新建人物

1. 进入企划详情页。
2. 点击“人物设定”。
3. 点击新增人物。
4. 在详情弹窗中填写角色信息。
5. 内容会自动保存。

### 多版本管理

同一人物可以有多个版本，例如“少年期”“觉醒后”“受伤后”。新建版本时，系统会复制当前版本作为基础，再生成新的版本号。后续 AI 扩写和资产匹配默认优先使用较新的版本。

### 生成人物形象图

配置图片 API 后，可以基于人物外貌描述生成形象图。生成前可以调整模型、比例、尺寸、参考图和提示词。生成结果会写回人物档案，也会记录到资产库。

### 上传人物图片

配置 COS 后，可以上传本地图片作为人物形象图或参考图。上传后的图片会获得公网地址，可继续用于图片生成和视频生成。

### 生成人物音色

配置音频 API 后，可以为人物生成音色。人物音色可在视频提示词中作为音色参考，帮助有声视频保持角色声音一致。

## 物品设定

物品设定用于管理故事中的道具、武器、载具、法器、装置等。

物品档案通常包含：

- 名称。
- 分类。
- 外观描述。
- 功能用途。
- 来源背景。
- 版本标签。
- 物品形象图。
- 参考图。

使用方式与人物设定类似：可以新增物品、编辑详情、创建不同版本、生成形象图或上传图片。后续在分镜中使用 `@物品名` 标注后，资产准备阶段可以自动匹配这些设定。

## 场景设定

场景设定用于管理故事中的地点、空间和环境。

场景档案通常包含：

- 名称。
- 分类。
- 外观描述。
- 光影氛围。
- 来源背景。
- 版本标签。
- 场景形象图。
- 参考图。

场景设定会被用于故事扩写、资产准备和视频生成。例如在分镜画面描述中标注 `@废弃车站`，资产准备阶段可以从场景设定中匹配该场景，并将场景图用于后续视频参考。

## 风格设定

风格设定用于选择当前企划采用的视觉风格。选择风格后，资产生成、图片生成、故事板生成和视频生成会自动引用对应提示词模板。

使用方式：

1. 进入企划详情页。
2. 点击“风格设定”。
3. 在风格卡片中选择一种风格。
4. 系统自动保存选择。

如需新增或编辑风格，请进入首页的“风格模板”页面。

## 剧集编辑流程

每个剧集采用四步创作流程：

```text
内容扩写 -> 分镜生成 -> 资产准备 -> 视频生成
```

剧集编辑器顶部有步骤导航，可以在四个步骤之间切换。内容会自动保存，页面右上角会显示保存状态。

## 第一步：内容扩写

内容扩写用于把简短灵感扩展为更完整的故事内容。

### 输入原始内容

在“第一步 · 输入故事内容”中输入故事梗概、剧情片段或想表达的内容。例如：

```text
一个雨天，女孩在咖啡馆等一个不会来的人，窗外雨声渐大，她慢慢喝完最后一口咖啡。
```

输入框支持 `@` 提及，可引用世界设定、人物、物品、场景等上下文。

### AI 扩写

点击“AI 扩写”后，如果当前企划已有世界设定、人物设定、物品设定、场景设定或前序剧集，系统会弹出上下文选择框。勾选需要引用的上下文后，AI 会基于这些内容扩写故事。

扩写过程中可以点击“停止”。扩写结果会显示在“扩写结果”区域，并且可以手动修改。

### AI 优化

原始内容和扩写结果旁边都有 AI 优化按钮。可用于润色文字、改善表达或补充细节。

### 提取人物设定

扩写完成后，点击“提取人物设定”，系统会从扩写结果中识别人物并写入当前企划的人物设定。遇到已存在人物时，会根据冲突处理方式新增、覆盖或跳过。

### 生成分镜

点击“生成分镜”，系统会根据扩写内容自动拆解为结构化分镜，并进入第二步。

## 第二步：分镜生成

分镜生成页以表格形式展示每个镜头。

### 分镜字段

| 字段 | 说明 |
| --- | --- |
| 时长 | 当前镜头预计时长 |
| 画面描述 | 画面中发生的事情，可使用 `@标签` 标注人物、场景、物品 |
| 景别 | 特写、近景、中景、全景、远景等 |
| 光影氛围 | 镜头的光线、色调和情绪 |
| 对白旁白 | 角色对白、旁白或字幕内容 |
| 音效 | 环境声、动作声、音乐提示等 |
| 运镜 | 推、拉、摇、移、跟、固定等 |

所有字段都可以手动编辑。

### 智能标注

点击“智能标注”，系统会识别画面描述中的人物、场景和物品，并用 `@标签` 标记出来。第三步会根据这些标签准备资产。

也可以在单行画面描述中手动输入 `@`，选择或创建标签。

### 重新生成分镜

点击“重新生成分镜”会基于当前扩写内容重新拆分镜头。此操作会覆盖已有镜头、已编辑内容、`@` 标注和资产关联，执行前会二次确认。

### 添加和调整镜头

分镜表支持：

- 手动添加镜头。
- 智能添加镜头。
- 删除镜头。
- 上移或下移镜头。
- 单行智能标注。

### 导出分镜

点击“导出 JSON”可以下载当前剧集的分镜数据，包含剧集标题、原始内容、扩写内容和镜头表格。

## 第三步：资产准备

资产准备用于把分镜中的 `@标签` 转换为可用于图片和视频生成的角色、场景、物品资产。

### 标签识别

页面会显示第二步识别出的所有 `@标签`，并提示哪些标签尚未生成资产。

### 从设定中匹配添加

点击“从设定中匹配添加”，系统会按名称从当前企划的人物设定、物品设定、场景设定中匹配资产。如果找到同名设定，会直接创建资产并继承设定中的描述和图片。

### 一键生成资产信息

点击“一键生成资产信息”，AI 会为未匹配的标签判断类型，并生成用于图片生成的中文提示词。

适用场景：

- 分镜里出现了新人物，但人物设定中还没有该角色。
- 分镜里出现了新道具或新地点。
- 希望快速生成资产描述，而不是手动填写。

### 手动添加资产

可以手动添加人物、场景或物品资产，也可以从已有设定中选择添加。

### 编辑资产描述

每个资产卡片都可以编辑名称、类型和描述。描述会作为图片生成提示词的基础内容。

### 生成资产图片

配置图片 API 后，可以为单个资产生成图片。生成弹窗中可调整：

- 提示词。
- 模型。
- 图片比例。
- 尺寸。
- 参考图。
- 风格模板。

生成完成后，图片会写回资产卡片，并记录到资产库。

### 上传本地图片

配置 COS 后，可以上传本地图片作为资产图片。上传成功后图片会获得公网地址，可用于视频生成参考图。

## 第四步：视频生成

视频生成页中，每张卡片对应一个分镜镜头。

### 生成视频提示词

点击镜头卡片中的提示词生成按钮后，系统会结合：

- 镜头画面描述。
- 景别、运镜、光影、对白、音效。
- `@标签` 关联的资产描述。
- 当前企划风格模板。
- 角色音色信息。

生成该镜头的视频提示词。提示词生成后仍可手动修改。

### 资产关联

系统会根据镜头画面描述中的 `@标签` 自动关联第三步生成的资产。用户也可以手动添加或移除关联资产。

关联资产的图片会作为首帧、尾帧或参考图参与视频生成，具体取决于所选模型和生成模式。

### 视频生成模式

支持的模式由所选视频模型能力决定，常见模式包括：

| 模式 | 说明 |
| --- | --- |
| 文生视频 | 仅根据提示词生成视频 |
| 图生视频-首帧 | 使用一张图片作为首帧参考 |
| 图生视频-首尾帧 | 使用首帧和尾帧控制视频起止画面 |
| 多模态参考生视频 | 使用多张图片、视频或音频作为参考素材 |

### 视频参数

每个镜头可以单独配置：

- 视频模型。
- 生成模式。
- 分辨率。
- 画幅比例。
- 时长。
- 是否生成音频。
- 种子值。
- 固定镜头。
- 水印。
- 优先级。
- 草稿模式。
- 参考图片、参考视频和参考音频。

参数会根据模型能力自动收敛，避免选择模型不支持的选项。

### 生成视频

1. 确认镜头提示词已生成。
2. 检查关联资产和参考图。
3. 点击“生成视频”。
4. 等待异步任务完成。
5. 成功后可在卡片中预览视频。

视频生成通常需要等待一段时间。进行中的任务支持刷新或切页后恢复状态。

### 批量生成

页面支持按镜头逐个生成，也支持批量处理已有提示词的镜头。批量生成会依次提交待生成的视频任务。

### 取消任务

排队中或生成中的视频任务可以取消。部分供应商会调用上游取消接口，部分供应商会在本地标记为取消。

### 截取尾帧

视频生成成功后，可以截取视频最后一帧并保存为截屏资产。该截屏可作为后续镜头的首帧或参考图，适合保持镜头衔接。

使用截屏功能前需要配置 COS，并确保视频地址支持跨域访问。

### 故事板图片

视频生成页还支持为镜头生成故事板图片。故事板图片会保存为资产，并可加入该镜头参考图列表。

## 音乐生成

音乐功能位于企划详情页的“音乐”标签。

### 新建音乐

1. 进入企划详情。
2. 切换到“音乐”标签。
3. 点击“新建音乐”。
4. 进入音乐编辑页。

使用前需要先在设置页配置音乐生成 API。

### 音乐模式

音乐编辑页支持三种模式：

| 模式 | 用途 |
| --- | --- |
| 灵感模式 | 输入音乐风格和氛围，由 AI 生成音乐 |
| 自定义模式 | 输入标题、歌词、风格标签等，生成更可控的歌曲 |
| 二次创作 | 选择已有音乐、上传本地音频或填写公网音频 URL，基于源音乐再创作 |

### 灵感模式

适合快速生成背景音乐或氛围音乐。

使用方式：

1. 选择版本。
2. 输入提示词，例如“深夜城市 lo-fi 钢琴，带雨声”。
3. 选择是否生成纯音乐。
4. 可指定男声、女声或不指定。
5. 点击“生成音乐”。

### 自定义模式

适合生成完整歌曲。

可填写：

- 标题。
- 歌词。
- 风格标签。
- 负面标签。
- 是否纯音乐。
- 人声性别。
- 是否对输入歌词进行二次创作。
- 歌手音色。
- 权重参数。

如果没有现成歌词，可以填写歌词主题，点击“AI 生成歌词”。

### 二次创作

二次创作支持三种源音乐来源：

| 来源 | 使用方式 |
| --- | --- |
| 从资产库选择 | 从已有音乐资产中选择 |
| 本地上传 | 上传本地音频文件，需要配置 COS |
| 添加公网地址 | 粘贴公开可访问的音频 URL |

选择源音乐后，可以填写标题、歌词或提示词、风格标签、负面标签等，再点击“生成音乐”。

### 查看结果

生成完成后，页面会显示音轨列表，包括：

- 封面。
- 标题。
- 时长。
- 风格标签。
- 音频播放器。
- 歌词展开/收起。

生成失败时，页面会显示失败原因，可以修改参数后重新生成。

## 资产库

资产库用于统一管理应用中生成或手动添加的媒体素材。

### 支持的资产类型

资产库支持查看：

- 图片。
- 视频。
- 音色。
- 音乐。
- 歌手音色。

图片资产还可以按实体类型筛选：

- 人物。
- 物品。
- 场景。
- 截屏。
- 故事板。
- 生成图。

### 筛选资产

可以按以下条件筛选：

- 企划。
- 媒体类型。
- 实体类型。

当选择视频或歌手音色时，实体类型筛选会自动失效。

### 添加资产

点击“添加资产”可以手动添加图片、视频或音频资产。上传本地文件时需要配置 COS。

### 创建歌手音色

点击“创建歌手音色”可以创建用于音乐生成的歌手音色。创建后的音色会出现在资产库中，也可在音乐自定义模式中选择。

### 资产操作

资产卡片支持常见操作：

- 预览图片或视频。
- 播放音频或音乐。
- 复制 URL。
- 下载媒体文件。
- 删除单个资产。
- 多选后批量删除。

## 预设库

预设库用于管理可复用素材，适合保存常用参考图、参考视频、音频样例和文本提示词。

### 预设类型

预设库支持：

- 图片预设。
- 视频预设。
- 音频预设。
- 文本预设。

### 添加预设

1. 点击“添加预设”。
2. 选择预设类型。
3. 填写名称。
4. 上传文件或填写文本内容。
5. 选择或创建标签。
6. 保存。

上传图片、视频或音频文件时需要配置 COS。

### 标签管理

点击“管理标签”可以新增或删除预设标签。标签用于筛选和组织预设素材。

### 筛选和搜索

预设库支持：

- 按类型筛选。
- 按名称搜索。
- 按标签筛选。

### 多选管理

点击“多选”后可以选择多个预设，支持全选、清空和删除选中项。

## 风格模板

风格模板用于控制不同类型资产和镜头生成时的视觉风格。

### 模板内容

每个风格模板通常包含：

- 风格名称。
- 风格描述。
- 人物图片提示词模板。
- 场景图片提示词模板。
- 物品图片提示词模板。
- 故事板图片提示词模板。
- 人物参考图。
- 场景参考图。
- 物品参考图。

### 新建和编辑模板

进入首页“风格模板”页面后，可以新建模板、复制模板、编辑模板内容或删除自定义模板。内置模板通常作为基础风格，不建议直接删除。

### 参考图

人物、场景、物品模板支持参考图。可以通过图片 API 生成参考图，也可以上传本地图片。上传本地图片需要配置 COS。

### 在企划中使用模板

创建或编辑模板后，进入某个企划的“风格设定”页面选择该模板。后续资产图片、故事板图片和视频提示词会引用选中的风格。

## 设置页说明

设置页用于管理所有外部服务和默认参数。

### LLM 设置

用于文本相关能力，包括：

- 故事扩写。
- 分镜生成。
- 智能标注。
- 提取人物设定。
- 资产信息生成。
- 视频提示词生成。
- AI 优化。
- AI 生成歌词。

支持 DeepSeek、GLM、小米 MiMo、火山方舟、APIMart 和自定义 OpenAI 兼容接口。

### 图片 API

用于：

- 人物形象图。
- 物品形象图。
- 场景形象图。
- 资产图片。
- 故事板图片。
- 风格参考图。

设置页可配置默认图片生成参数，后续生成弹窗会默认带入这些参数。

### 视频 API

用于镜头视频生成。设置页可配置默认视频生成参数，包括模型、比例、分辨率、时长、是否有声等。

### 音频 API

用于人物音色生成。

### 音乐 API

用于灵感模式、自定义模式和二次创作音乐生成。

### COS 存储

COS 用于把本地上传文件或生成结果转为可公开访问的 URL。建议配置：

- SecretId。
- SecretKey。
- Bucket。
- Region。
- 自定义 CDN 域名。

未配置 COS 时，部分上传、本地文件引用、视频持久化和截屏功能会受限。

## 推荐使用流程

### 从零开始创作一集视频

1. 进入“设置”，配置 LLM、图片 API、视频 API 和 COS。
2. 在首页点击“新建企划”。
3. 进入企划后填写“世界设定”。
4. 创建人物、物品、场景设定，也可以先跳过。
5. 进入“风格设定”，选择合适的风格模板。
6. 在“剧集”标签中新建剧集。
7. 第一步输入剧情梗概，点击“AI 扩写”。
8. 检查并编辑扩写结果。
9. 点击“提取人物设定”，把主要角色写入人物档案。
10. 点击“生成分镜”。
11. 第二步检查镜头表，点击“智能标注”。
12. 第三步点击“从设定中匹配添加”或“一键生成资产信息”。
13. 为关键人物、场景、物品生成或上传图片。
14. 第四步生成视频提示词。
15. 检查每个镜头的参考图、模式和参数。
16. 逐个或批量生成视频。
17. 对需要衔接的镜头截取尾帧，作为下一个镜头参考。
18. 在资产库中统一查看生成的视频、图片和音频。

### 为企划生成音乐

1. 进入设置页配置音乐 API。
2. 如需上传或保存本地音频，配置 COS。
3. 进入企划详情，切换到“音乐”标签。
4. 点击“新建音乐”。
5. 选择灵感模式、自定义模式或二次创作。
6. 填写提示词、歌词、标签或源音乐。
7. 点击“生成音乐”。
8. 在结果区试听音轨，展开查看歌词。

## 常见问题

### 为什么无法新建企划或新建剧集

通常是因为还没有配置 LLM。请进入“设置”页面填写 LLM API Key、模型和接口地址。

### 为什么无法生成图片

请检查：

- 图片 API 是否已配置。
- 当前选择的图片模型是否有效。
- API Key 是否正确。
- 如果使用本地上传或参考图，COS 是否已配置。

### 为什么无法生成视频

请检查：

- 视频 API 是否已配置。
- 当前镜头是否已经生成视频提示词。
- 参考图片是否为公网可访问地址。
- 所选视频模型是否支持当前模式、比例、分辨率和时长。

### 为什么本地上传失败

本地上传依赖 COS。请先在设置页配置腾讯云 COS，并确认 Bucket、Region、SecretId、SecretKey 可用。

### 为什么截取尾帧失败

截取尾帧需要视频 URL 支持跨域访问。请检查 COS 存储桶的 CORS 设置，允许当前应用域名访问视频文件。

### 为什么刷新后任务还在生成

图片、故事板和视频生成都是异步任务。系统会保存任务 ID，刷新或切换页面后会尝试恢复轮询，因此任务不会因为短暂离开页面而立即丢失。

### 资产库和预设库有什么区别

资产库主要记录创作过程中生成或手动添加的实际媒体成果，例如角色图、场景图、视频、音乐和音色。预设库主要保存可复用的参考素材或提示词，便于在后续创作中快速选择。

### 风格设定和风格模板有什么区别

风格模板是全局可管理的模板库，定义人物、场景、物品和故事板的提示词规则。风格设定是某个企划当前选用哪个模板。

## 使用建议

- 先写世界设定，再做扩写，可以让多集内容更稳定。
- 主要人物建议先在人物设定中建立档案，再进入剧集创作。
- 分镜中尽量使用一致的 `@标签` 名称，例如始终使用 `@林夏`，不要在不同镜头中混用 `@女孩`、`@女主` 和 `@林夏`。
- 关键角色、关键场景建议先生成高质量参考图，再生成视频。
- 视频生成前先检查每个镜头的提示词和参考图，避免批量生成后重复返工。
- 生成视频后及时截取尾帧，可帮助下一个镜头保持视觉连续。
- 常用参考图、参考视频和提示词可以沉淀到预设库中，方便跨企划复用。
